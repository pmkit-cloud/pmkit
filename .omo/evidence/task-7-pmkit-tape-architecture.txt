Todo 7 evidence
===============

Public opt-in API
-----------------
Pmkit::builder(runtime).storage(Arc<dyn TapeStore>)

The omitted-storage path passes None into every driver and does not call storage.
Configured storage persists only SourceEnvelope::PmMarket and SourceEnvelope::PmAccount.
SourceEnvelope::CexReference, watermarks, and EOF are explicit no-ops.

External consumer
-----------------
Command:
  cargo run --manifest-path .omo/evidence/consumer/Cargo.toml
Result:
  todo7 consumer: backtest/paper/live snapshots match

The fixture-backed backtest, paper, and live-fixture modes use the same PM/CEX
signals. The consumer verifies equal snapshots, PM market/account raw-frame byte
identity, canonical PM cursor order, and a durable causal-decision record.

Command:
  cargo run --manifest-path .omo/evidence/consumer/Cargo.toml -- --store-fails
Result:
  todo7 consumer: typed pre-submission storage failure aborted submission

Raw adapter proof
-----------------
RawPolymarketFrameAdapter persists exact text before invoking its adapter closure.
Its focused tests verify stored byte identity and that a configured store failure
prevents adaptation/emission.

Workspace gates
----------------
  cargo fmt --all -- --check
  cargo clippy --workspace --all-targets -- -D warnings
  cargo test --workspace
  cargo doc --workspace --no-deps

Result: formatting and Clippy passed; 110 tests across 39 suites passed; docs generated.
